import { Sensitivity } from '../common/Sensitivity';
export declare class RuleDefaultSensitivity {
    min: Sensitivity;
    max: Sensitivity;
    defaultValue: Sensitivity;
    constructor(min?: Sensitivity, max?: Sensitivity, defaultValue?: Sensitivity);
}
