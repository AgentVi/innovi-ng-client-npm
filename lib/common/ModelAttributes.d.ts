import { Attribute } from '../common/Attribute';
export declare class ModelAttributes {
    modelName: string;
    attributes: Attribute[];
    constructor(modelName?: string, attributes?: Attribute[]);
}
