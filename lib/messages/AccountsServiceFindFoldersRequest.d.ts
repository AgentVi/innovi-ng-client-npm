export declare class AccountsServiceFindFoldersRequest {
    parentId: string;
    search: string;
    sort: string;
    page: number;
    pageSize: number;
    constructor(parentId?: string, search?: string, sort?: string, page?: number, pageSize?: number);
}
