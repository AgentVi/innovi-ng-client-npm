export declare class OnvifChannel {
    uUID: string;
    name: string;
    address: string;
    sensorId: string;
    constructor(uUID?: string, name?: string, address?: string, sensorId?: string);
}
